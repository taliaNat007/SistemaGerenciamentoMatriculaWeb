create database gerenciamentoaluno;
USE gerenciamentoaluno;

SELECT * FROM fornecedores;

INSERT INTO fornecedores (
    nome,
    ativo,
    criado_em,
    atualizado_em
)
VALUES
('Fornecedor A', 1, NOW(), NOW()),
('Fornecedor B', 1, NOW(), NOW()),
('Fornecedor C', 0, NOW(), NOW());